#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point
* printf - prints out the numbers
* Return: Always 0 (Success)
*/

int main(void)
{
	int i;
	i = 0;
	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}

